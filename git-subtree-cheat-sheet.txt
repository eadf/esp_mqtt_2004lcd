git stash

git checkout mqtt_branch
git pull
git checkout master
git merge --squash -s subtree --no-commit mqtt_branch
git commit -m "esp_mqtt upstream pull"

git checkout digoleserial_branch
git pull
git checkout master
git merge --squash -s subtree --no-commit digoleserial_branch
git commit -m "digoleserial upstream pull"

git stash apply
